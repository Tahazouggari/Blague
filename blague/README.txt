2---

	le code affiche :  x vaut 5.


	declaration d'un variable reference r qui refert a la variable x
	r prend la valeur de y qui est 3 donc la valeur de x change .
	r prend la valleur 5 donc la valeur de x change .
	
	
swapPageCount(lireLivre(),lireLivre()); 

   le console affiche " initial value of reference to non-const must be an value" cela veut dire que les resultats donner par les fonction max min ne conforme pas les argument de la fonction swap qui prend en argument deux reference.

